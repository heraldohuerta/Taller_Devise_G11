<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Pictory</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
        <% if current_user.admin == true  %>
          <li><h1 class="navbar-brand">Admin</h1></li>
          <li><h3 class="navbar-brand"> <%= link_to "Modificar Usuarios",  index_path %></h3></li>

        <% end %>
         <li><h1 class="navbar-brand">[Bienvenido <%=  current_user.name  if user_signed_in?   %> </h1></li>
         <li><h1 class="navbar-brand"> <%=  current_user.email if user_signed_in?    %> </h1></li>
         <li><h1 class="navbar-brand"> <%=  current_user.id.to_s  if user_signed_in?  %>] </h1></li>
         <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
              <li><%= link_to "Mis Historias",  my_history_path(current_user.id) %></li>
              <li><%= link_to "Cerrar Sesion", destroy_user_session_path, method: :delete %></li>

      <% else %>
              <li><%= link_to "Registrarse",  new_user_registration_path %></li>
              <li><%= link_to "Iniciar Sesion", new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
